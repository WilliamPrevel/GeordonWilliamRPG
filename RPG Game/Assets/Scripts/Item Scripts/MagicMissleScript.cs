using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MagicMissleScript : arrowScript {

	// Use this for initialization
	void Start () {
        movespeed = 0.1f;
        killtime = 30;
        power = 30;
}
	
	// Update is called once per frame
	void Update () {
		
	}
}
